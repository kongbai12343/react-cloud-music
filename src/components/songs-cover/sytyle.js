import styled from "styled-components";

export const SongsCoverWrapper = styled.div`
  display: flex;

  .cover-top {
    padding: 20px 0 20px 0;
    position: relative;
  }

  .cover-bottom {
    width: 140px;
    font-size: 14px;
  }
`

